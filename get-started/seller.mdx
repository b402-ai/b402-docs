---
title: Seller Guide
description: 'Accept payments from buyers'
icon: 'coin-vertical'
---

The **b402 protocol** enables you to monetize your APIs or web services with **gasless EVM-compatible token payments** across supported EVM chains.

This guide walks you through integrating the `b402-express` middleware to create paid API routes that require crypto payments before access.

Currently, a TypeScript SDK is available for developers. Additional SDKs including Python will be released soon.


<Info>
  b402 does not require you to set up API keys. The b402 facilitator verifies signatures and submits transactions.
</Info>

## Installation

First, install the seller middleware package:

``` bash
npm install b402-express
```

## Basic Setup

Import and configure the middleware in your Express app, specifying your wallet address and the services you want to sell:

```ts lines
const express = require('express');
const { b402 } = require('b402-express');

const app = express();

app.use(b402(
  { payTo: '0x...' }, // your wallet address on a supported chain
  {
    'GET /premium': { price: '0.01', token: 'USD1' },
    'GET /basic': { price: '0.001', token: 'USDT' },
  }
));
```

## Route Configuration
### Protected/Paid Routes

Endpoints listed in the middleware configuration require payment before access.
When called without valid proof of payment, the middleware automatically returns an HTTP 402 (Payment Required) response containing all necessary payment details.

### Free Routes

Any route not listed in the configuration remains free and accessible as usual.

## Payment Details

When a protected endpoint is accessed without payment, the middleware returns a JSON payload with:
- Token contract address
- Your receiving wallet (payTo)
- Required amount
- Relayer contract address (e.g., `0x590162bf4b50F6576a459B75309eE21D92178A10`)
- Description of the payment

After payment, verified details become available in the request object:

``` ts lines
req.b402.txHash  // transaction hash on the configured chain
req.b402.payer   // payer’s wallet address
```

## Complete Example

```typescript
const express = require('express');
const { b402 } = require('b402-express');

const app = express();

// Configure b402 middleware with your wallet and pricing
app.use(b402(
  { payTo: '0xYourWalletAddress' },
  {
    'GET /premium-data': { price: '0.01', token: 'USD1' },
    'GET /analytics':    { price: '0.005', token: 'USDT' },
  }
));

// Paid route - requires payment
app.get('/premium-data', (req, res) => {
  console.log('Payment from:', req.b402.payer);
  console.log('TX hash:', req.b402.txHash);
  res.json({ data: 'premium content', timestamp: Date.now() });
});

// Free route - no payment required
app.get('/health', (req, res) => {
  res.json({ status: 'ok' });
});

app.listen(3000, () => {
  console.log('b402 server running on port 3000');
});
```

## Supported Tokens

For the complete list of supported tokens, see [Network & Token Support](/concepts/network-token-support).

## Next Steps

- [Buyer Guide](/get-started/buyer) - Understand the buyer flow
- [Facilitator API Reference](/developers/facilitator/overview) - Direct API integration
- [Architecture](/architecture) - Understand the full payment flow
